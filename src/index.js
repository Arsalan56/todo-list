import './style.css';
import './to-do-list.png';
import './add.svg';

const BarCheck = () => {
    if (window.innerWidth >= 900) {
        // console.log('hi');
    }
};

BarCheck();

// window.addEventListener('resize', BarCheck);
